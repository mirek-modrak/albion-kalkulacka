{
  "name": "albion-kalkulacka",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "jadro",
    "nastroje",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "generuj": "node nastroje/generuj-data.mjs",
    "test": "npm run test --workspace jadro && npm run test --workspace web",
    "kontrola": "npm run kontrola --workspace jadro && npm run kontrola --workspace web"
  }
}
